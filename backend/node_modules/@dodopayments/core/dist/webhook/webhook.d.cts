import { Resolve, HandlerWithContext, WebhookPayload, WebhookEventHandlers } from '../schemas/webhook.cjs';
import 'zod/v3';

/**
 * @module webhook
 * @description Server-only webhook verification utilities.
 * This module uses Node.js crypto and should only be imported in server contexts.
 * @see https://github.com/dodopayments/dodo-adapters for usage examples
 */

type WebhookHandlerConfig<TContext = void> = Resolve<{
    webhookKey: string;
    onPayload?: HandlerWithContext<TContext, WebhookPayload>;
} & WebhookEventHandlers<TContext>>;
declare function handleWebhookPayload(payload: WebhookPayload, config: WebhookHandlerConfig<void>): Promise<void>;
declare function handleWebhookPayload<TContext>(payload: WebhookPayload, config: WebhookHandlerConfig<TContext>, context: TContext): Promise<void>;
declare const verifyWebhookPayload: ({ webhookKey, headers, body, }: {
    webhookKey: string;
    headers: Record<string, string>;
    body: string;
}) => Promise<WebhookPayload>;

export { type WebhookHandlerConfig, handleWebhookPayload, verifyWebhookPayload };
